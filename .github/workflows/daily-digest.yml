name: Daily AI Digest

on:
  schedule:
    - cron: '0 6 * * 1-5'  # 6am UTC Mon–Fri only
  workflow_dispatch:        # allow manual trigger from GitHub UI

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: pipeline/package-lock.json

      - name: Install pipeline dependencies
        run: npm install
        working-directory: pipeline

      - name: Set day of week
        run: echo "DAY_OF_WEEK=$(date +%u)" >> $GITHUB_ENV

      - name: Generate feed
        run: node generate-feed.js
        working-directory: pipeline
        env:
          POD2TXT_API_KEY: ${{ secrets.POD2TXT_API_KEY }}

      - name: Prepare digest
        run: node prepare-digest.js
        working-directory: pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit and push digest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add web/data/digests/
          git diff --staged --quiet || (git commit -m "digest: $(date +%Y-%m-%d)" && git push)
